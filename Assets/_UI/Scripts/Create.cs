using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Create : MonoBehaviour
{
    
    /*[SerializeField] GameObject[] playerObjPool;

    public void CreatePlayer()
    {
        // 플레이어 오브젝트 생성
        GameObject player = Instantiate(playerFactory);

        // 생성된 플레이어 오브젝트를 풀에 추가
        AddToPlayerObjPool(player);
    }

    private void AddToPlayerObjPool(GameObject player)
    {
        // 오브젝트 풀에 플레이어 오브젝트 추가
        for (int i = 0; i < playerObjPool.Length; i++)
        {
            if (playerObjPool[i] == null)
            {
                playerObjPool[i] = player;
                break;
            }
        }
    }*/
}
